# Chronic Conditions Interoperability
# Shows medical history / chronic condition components shared across studies.
# Demonstrates that identically-modeled CDEs produce structurally compatible data
# for conditions like diabetes, hypertension, and heart disease.
#
# NOTE: This query expresses the TARGET cross-study join pattern. It uses an
# illustrative placeholder vocabulary; SDCStudio RDF uses sdc4-meta: predicates.
# It is not yet runnable against the current models: the three studies share no
# ct_ids because their concepts have not been canonicalized to common components
# (an expert-reviewed, human-in-the-loop step). See ../README.md, section
# "Current Limitations and Future Work".

PREFIX sdc4: <https://datacharterontology.org/sdc4/>
PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

SELECT ?ctId ?label ?study ?dataType ?description
WHERE {
    ?component a sdc4:Component ;
               sdc4:ctId ?ctId ;
               rdfs:label ?label ;
               sdc4:domain "Medical History" ;
               sdc4:study ?study ;
               sdc4:dataType ?dataType .
    OPTIONAL { ?component sdc4:description ?description . }
}
ORDER BY ?label ?study
